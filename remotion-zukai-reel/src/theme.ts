// ── 図解リールの配色（元動画を解析して抽出した3色＋グレー）──
// この色数を絞るのが「図解っぽさ」の最大のコツ。増やしすぎないこと。
export const COLORS = {
  bg: "#F4EFF2", // 淡いピンク/ラベンダーの背景
  ink: "#1B2A4A", // 線・文字のネイビー
  accent: "#E5432B", // 差し色の赤オレンジ
  gray: "#C9CDD6", // 塗りのグレー
  grayLight: "#E3E5EA", // 薄いグレー
  white: "#FFFFFF",
} as const;

// 線画の太さ（1080px幅を基準）
export const STROKE = 8;

// ── 文字サイズの基準スケール（参考動画を実測して1080×1920に換算・全ページ共通）──
// これを必ず使うことで、ページ間で文字サイズがバラつかない。
export const TYPE = {
  h1: 60, // 見出し（赤い縦線＋枠）
  h2: 48, // サブ見出し
  caption: 46, // 補足キャプション
  big: 96, // 強調の大きい数字
  badge: 36, // 上のバッジ
  bubble: 42, // 吹き出し
  note: 24, // 注記・免責
} as const;

// ── レイアウトの固定ゾーン（1920高さに対する割合）──
// 下ゾーン(84%〜)には文字を絶対に置かない。
export const ZONE = {
  headerTop: 96, // 上ゾーンの開始y
  stageTop: 430, // イラスト主役ゾーンの中心帯 開始
  stageBottom: 1300, // 主役ゾーン 終了
  captionCenter: 1450, // キャプションはこの辺（イラスト直下・中〜やや下）
  safeBottom: 1620, // これより下(〜1920)は文字禁止＝余白
} as const;
